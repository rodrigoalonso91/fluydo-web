---
interface Props {
  of: any[];
  render: any; // Recibe un Componente Astro
}

const { of, render: Component } = Astro.props;
---

{
  of.map((item, index) => (
    <Component item={item} index={index} />
  ))
}